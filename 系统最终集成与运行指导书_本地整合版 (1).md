# 系统最终集成与运行指导书（本地整合版）

> 目标：把 GitHub 仓库里四人已合并的全部代码，拉到**你这台电脑**上，跑成一个**真正能用的完整系统**——可以注册、登录、转大转盘、看历史。
> 适用：本地演示 / 答辩（不涉及公网部署）。
> 前置：A/B/C/D 四人的代码均已合入 GitHub 仓库的 main 分支。

---

## 核心概念（先读一遍，避免困惑）

- **GitHub 仓库 = 完整系统的「源代码」**，但它只存代码、不运行代码。
- **「做出系统」= 在一台机器上把这些代码跑起来。** 本指导书就是在你的电脑上完成这「最后一步」。
- 前后端分离，所以本地要**同时启动两个进程**：后端 Django（跑 /api/ 接口）+ Nginx（返回前端页面并反代接口），统一从 `http://127.0.0.1/` 访问。

整个过程 = 拉取完整代码 → 装环境 → 建库 → 启动后端 → 启动 Nginx → 全链路验收。

---

## 步骤 0：确认四人代码都已合入

在开始前，确认 GitHub 仓库 main 分支已包含四人成果：

- A：`backend/` 骨架、`app/common.py`、`app/models.py`、迁移与初始化数据、账号接口、`frontend/login.html`、`frontend/register.html`、`nginx/dinner.conf.example`
- B：`app/views.py`/`app/urls.py` 里的餐馆 `/api/restaurants/...` 接口、`frontend/admin.html`
- C：随机抽取 `/api/random-dinner/`、历史 `/api/history/` 接口
- D：`frontend/index.html`（大转盘）、`frontend/history.html`、公共 js/css（已对接真实接口）

> 若还有人没合入，先让其合入并 `git push`，否则跑起来会缺模块。

---

## 步骤 1：拉取完整代码到你的电脑

打开终端，进入你想存放项目的目录，克隆**整个仓库**（注意是完整仓库，含 backend / frontend / nginx，不是只拉你自己那部分）：

```bash
git clone https://github.com/你们组/dinner-decider.git
cd dinner-decider
```

> 若你之前已 clone 过，进入目录后拉最新即可：
> ```bash
> cd dinner-decider
> git checkout main
> git pull origin main
> ```

确认目录结构完整：

```bash
ls
# 应能看到： backend/  frontend/  nginx/  README.md  .gitignore
```

---

## 步骤 2：启动后端（终端 1）

```bash
cd backend

# 创建并激活虚拟环境
python -m venv .venv
source .venv/bin/activate          # Windows: .venv\Scripts\activate

# 安装依赖
pip install django

# 建库 + 写入初始化数据（默认管理员 + 10 家餐馆）
python manage.py migrate

# 启动后端，监听 8000
python manage.py runserver 127.0.0.1:8000
```

✅ 看到 `Starting development server at http://127.0.0.1:8000/` 即后端就绪。
**这个终端保持开着不要关。**

> 如有 `requirements.txt`，用 `pip install -r requirements.txt` 代替 `pip install django`。

---

## 步骤 3：配置并启动 Nginx（终端 2，新开一个）

### 3.1 生成本机配置

```bash
# 回到仓库根目录
cd dinner-decider
cp nginx/dinner.conf.example nginx/dinner.conf
```

### 3.2 修改配置里的前端路径

编辑 `nginx/dinner.conf`，把 `root` 改成**你电脑上 `frontend/` 的绝对路径**。

先查出绝对路径：

```bash
# 在仓库根目录执行
cd frontend && pwd        # 复制这行输出的路径，例如 /Users/you/dinner-decider/frontend
cd ..
```

把该路径填进配置：

```nginx
server {
    listen 80;
    server_name localhost;

    root /Users/you/dinner-decider/frontend;   # ← 改成你 pwd 查到的绝对路径
    index index.html;

    location /api/ {
        proxy_pass http://127.0.0.1:8000;
        proxy_set_header Host              $host;
        proxy_set_header X-Real-IP         $remote_addr;
        proxy_set_header X-Forwarded-For   $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }

    location / {
        try_files $uri $uri/ =404;
    }
}
```

### 3.3 把配置接入 Nginx 并启动

根据你的系统选其一：

**macOS（Homebrew 安装的 Nginx）：**
```bash
# 将配置 include 进主配置，或直接复制到 servers 目录
cp nginx/dinner.conf /opt/homebrew/etc/nginx/servers/dinner.conf
nginx                      # 启动
# 之后改了配置用： nginx -s reload
```

**Linux：**
```bash
sudo cp nginx/dinner.conf /etc/nginx/conf.d/dinner.conf
sudo nginx -t              # 测试配置语法
sudo systemctl restart nginx
```

**Windows：**
将 `dinner.conf` 的 server 块内容粘贴进 Nginx 安装目录的 `conf/nginx.conf` 的 `http { }` 内，然后在 Nginx 目录执行 `start nginx`（重载用 `nginx -s reload`）。

> 若 80 端口被占用（提示 `Address already in use`），可把配置里的 `listen 80;` 改成 `listen 8080;`，访问入口相应变成 `http://127.0.0.1:8080/`。

---

## 步骤 4：打开系统

浏览器访问**统一入口**：

```
http://127.0.0.1/
```

（若改了端口则是 `http://127.0.0.1:8080/`）

> ⚠️ 不要直接访问 `http://127.0.0.1:8000`（那是后端裸接口），也不要用 `file://` 打开 html，否则同源与反代都不成立、登录态会失效。

---

## 步骤 5：全链路验收（逐项打勾）

按下表完整走一遍，全部通过即系统集成成功。这条链路对应开发规范第八章「全链路流程」。

| # | 操作 | 预期结果 | ✓ |
|---|------|----------|---|
| 1 | 打开 `http://127.0.0.1/register.html` 注册新用户 | 提示注册成功 | ☐ |
| 2 | 用新用户在 login 页登录 | 登录成功，进入主页 | ☐ |
| 3 | 登出后用 `admin / admin123` 登录 | 登录成功，可见管理入口 | ☐ |
| 4 | 进管理页（admin.html）新增几家餐馆，设不同权重 | 列表出现新餐馆 | ☐ |
| 5 | 在管理页禁用其中一家餐馆 | 该餐馆状态变为禁用 | ☐ |
| 6 | 回主页点大转盘随机抽取若干次 | 转盘动画播放并揭晓结果 | ☐ |
| 7 | 观察多次抽取结果 | 被禁用的那家**始终不出现** | ☐ |
| 8 | 把某餐馆权重调到很高再多次抽取 | 该餐馆**明显更常被抽中** | ☐ |
| 9 | 打开历史页（history.html） | 能看到刚才每次抽取记录 | ☐ |
| 10 | 删除一家「已被抽中过」的餐馆（软删除）后看历史 | 该餐馆从管理列表和抽取中消失，但历史记录仍正常显示它（数据软删除保留） | ☐ |
| 11 | 点登出 | 退出登录，再调 /api/me/ 返回未登录 | ☐ |

全部打勾 → **系统集成完成，这台电脑上跑着的就是四人合起来的完整成品。**

---

## 日常重启（之后再次运行）

环境已配好后，下次运行只需两步（无需重新建虚拟环境/migrate，除非代码有更新）：

```bash
# 终端1：后端
cd dinner-decider/backend
source .venv/bin/activate
python manage.py runserver 127.0.0.1:8000

# 终端2：Nginx（若未在运行）
nginx                # 或 nginx -s reload
```

> 若期间队友更新了代码：先 `git pull origin main`；若改了 models 则再 `python manage.py migrate`。

---

## 常见问题排查

| 问题 | 原因 | 解决 |
|------|------|------|
| 打开 `http://127.0.0.1/` 是 Nginx 欢迎页/404 | `root` 路径没指向 frontend/ | 改 `dinner.conf` 的 root 为 frontend 绝对路径，`nginx -s reload` |
| 页面能开但点按钮无反应/接口报错 | 后端没启动，或 /api/ 反代没配 | 确认终端1 后端在跑；检查 `location /api/` 配置 |
| 登录后仍提示未登录 | 没从 Nginx 入口访问，或 fetch 没带 credentials | 从 `http://127.0.0.1/` 访问；确认前端 fetch 带 `credentials:'same-origin'` |
| POST 接口报 403 CSRF | 后端接口未加 @csrf_exempt | 让对应接口加 `@csrf_exempt`（开发期） |
| `nginx: [emerg] Address already in use` | 80 端口被占 | 改 `listen 8080;`，从 `http://127.0.0.1:8080/` 访问 |
| migrate 报 no such table / 抽取报错 | 没 migrate 或缺数据 | `cd backend && python manage.py migrate` |
| 大转盘抽取总是同几家 / 缺餐馆 | 餐馆没加或都被禁用 | 用 admin 登录在管理页补充并启用餐馆 |
| clone 后没有 nginx/dinner.conf | 它被忽略，仓库只有 .example | `cp nginx/dinner.conf.example nginx/dinner.conf` 再改路径 |

---

## 一句话总结

你并没有遗漏任何开发工作——只是还差「**在一台机器上把合并后的代码跑起来**」这一步。按本指导书在你电脑上 clone 完整仓库、启动后端 + Nginx、从 `http://127.0.0.1/` 走通全链路，四人的代码就第一次「合体显形」为一个真正能用的系统。
