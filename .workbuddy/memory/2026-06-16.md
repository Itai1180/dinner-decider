# 2026-06-16

## 随机晚饭决定器系统 — 开发规范提示词文档生成

- 生成了《随机晚饭决定器系统_开发规范_提示词.docx》（约45KB），存放于项目根目录
- 文档结构参照「搭子生命周期管理系统_开发规范.docx」的格式
- 内容包含九大章节：提示词总览、系统功能详述、数据模型设计要求、接口设计要求、技术架构要求、输出格式规范、敏捷迭代要求、精准表述约束、完整提示词
- 技术栈：Nginx → Django + DRF → SQLite/MySQL
- 核心管理对象：restaurant（餐馆/菜品表）、spin_record（抽取记录表）
- 10个API接口设计（CRUD + 权重管理 + 大转盘抽取 + 采纳 + 历史）
- 大转盘核心算法：加权随机（weight / sum(weights)）
- 敏捷迭代分为4个Sprint：基础骨架 → 大转盘核心 → 管理后台 → 体验优化
- 第九章包含可直接复制给AI的完整提示词文本

## 随机晚饭决定器后台系统 — 纯提示词版本生成

- 根据用户反馈，重新生成纯提示词版本文档《随机晚饭决定器后台系统_开发规范_提示词.docx》（约42KB）
- 去掉了九章节框架包装，文档本身就是可直接复制给AI的提示词
- 提示词结构：角色与任务 → 系统概述 → 技术栈 → 数据模型（含字段表格）→ 核心功能 → 接口清单（10个）→ 统一返回格式 → 文档输出要求 → 表述约束（9条硬约束）→ 敏捷迭代分期
- 明确了系统为纯后端API服务，前端页面不在本规范覆盖范围内
- 单用户模式不建user表，restaurant与spin_record两张核心表
